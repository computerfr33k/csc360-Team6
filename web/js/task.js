  function Task(id, subject, title, dueDate, completed, notify) {
    this.id = id;
    this.subject = subject;
    this.title = title;
    this.dueDate = dueDate;
    this.completed = completed;
    this.notify = notify;
  };
